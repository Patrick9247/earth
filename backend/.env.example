# 数据库配置
# 使用 SQLite（默认，无需额外配置）
USE_SQLITE=true
SQLITE_DB_PATH=./data/geothermal.db

# 使用 MySQL（如需MySQL，取消注释并配置）
# USE_SQLITE=false
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=geothermal_db
